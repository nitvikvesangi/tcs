# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1

# --- Data validation / settings ---
pydantic==2.10.3
pydantic-settings==2.6.1

# --- Database ---
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10

# --- Auth ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4's bcrypt backend detection breaks on bcrypt>=4.1 (missing
# `__about__`); pin to a version verified working with this passlib release.
bcrypt==4.0.1
python-multipart==0.0.19

# --- Data / ML ---
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.5.2
joblib==1.4.2

# --- HTTP client (weather/competitor/LLM calls) ---
httpx==0.28.1

# --- Config ---
python-dotenv==1.0.1

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.24.0
